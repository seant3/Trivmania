

// export default function QuestionDisplay({allChoices}) {
    
        
    
//     return (
//         allChoices.map((answer, i) =>
//         <div key={i}>
//             <button key={i} onClick={() => verifyAnswer(answer)}>
//             {answer}
//             </button>
//         </div>)
//         )
// }